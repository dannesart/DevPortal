/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SectionInFocusDirective } from './section-in-focus.directive';

describe('Directive: SectionInFocus', () => {
  it('should create an instance', () => {
    const directive = new SectionInFocusDirective();
    expect(directive).toBeTruthy();
  });
});
